#include <iostream>
#include <cctype>
using namespace std;

int main()
{
    /*char ch; //Un-Comment this to run this section ..
    ch = cin.get();

    if(isupper(ch))
    {
        cout<<"Upper-Case Letter"<<endl;
    }
    else if(islower(ch))
    {
        cout<<"Lower-Case Letter"<<endl;
    }
    else if(isdigit(ch))
    {
        cout<<"Numeric Digit"<<endl;
    }
    else if(iswspace(ch))
    {
        cout<<"White-Space"<<endl;
    }
    else
    {
        cout<<"Special Symbol"<<endl;
    }*/

    /*char c; //Un-Comment this to run this section ..
    cout<<"Enter character"<<endl;
    c = cin.get();
    cout<<(char)toupper(c)<<endl;
    cout<<(char)tolower(c)<<endl;*/
    return 0;
}
